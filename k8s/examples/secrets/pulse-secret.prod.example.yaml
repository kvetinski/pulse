apiVersion: v1
kind: Secret
metadata:
  name: pulse-secrets
  namespace: pulse-prod
type: Opaque
stringData:
  # Placeholder only. Replace from a secret manager; do not commit credentials.
  # Current Pulse has no custom Redis TLS certificate configuration path.
  PULSE_REDIS_URL: redis://replace-with-managed-prod-redis:6379
