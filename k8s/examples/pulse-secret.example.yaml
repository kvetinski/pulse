apiVersion: v1
kind: Secret
metadata:
  name: pulse-secrets
  namespace: pulse
type: Opaque
stringData:
  # If your Redis requires auth, use:
  # redis://:<password>@redis:6379
  PULSE_REDIS_URL: redis://redis:6379
